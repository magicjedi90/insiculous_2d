# Progress Log - Insiculous 2D

**Format:** `[YYYY-MM-DD HH:MM] AGENT-ID | TASK-XXX | Summary`

Agents: append entries here after pushing successfully. Most recent at top.

---

## Completed Tasks

(No tasks completed yet - agents will log progress here)

<!-- Example entries:
[2026-02-07 14:32] agent-01 | TASK-001 | Wired up viewport click-to-select in EditorGame::update(). 3 new tests, all 601 passing.
[2026-02-07 15:10] agent-02 | TASK-012 | Fixed double iteration in TransformHierarchySystem. Single-pass now. Added 5-level depth test. 599 passing.
-->
[2026-06-10] claude | EDITOR-AUDIT | Full DRY/SRP/KISS remediation of editor + editor_integration. Component dispatch unified into `editor_component_registry!` macro in stored_component.rs (was duplicated across 9+ sites; adding a component is now 1 line). Five *EditResult structs replaced by generic ComponentEdit<T> + apply_component_edit(); five Set*Commands collapsed into impl_set_component_command! macro. All 6 oversized files split (commands/, context/, editor_game/, panel_renderer/, field_style.rs); 28 hardcoded colors routed through EditorTheme (GizmoPalette, EditableFieldStyle tokens; menu/toolbar/hierarchy take &EditorTheme); magic numbers extracted (DEFAULT_SCENE_PATH, ranges mod, strides, constants.rs). MenuBar.render() split into 4 phases (layout pure + tested); gizmo render_axis_handle()/begin_drag_if() extracted. Clippy 0 warnings in both crates, 0 dead_code allows, 0 files >600 lines. TECH_DEBT.md: MAGIC-001/002/003, SRP-002, ARCH-001/002 resolved; editor_integration TECH_DEBT.md created. 879 workspace tests passing (editor 226, editor_integration 63).
[2026-06-11] claude | AUDIO-PHYSICS-AUDIT | Full DRY/SRP/KISS audit + remediation of audio and physics crates (2 parallel agents). PHYSICS: fixed 3 collision-event bugs (stale events re-emitted on no-step frames, sub-step event loss, contact points computed in collider-local space instead of world) — clearing moved out of step() into PhysicsSystem::update() via clear_collision_events(), step() now appends, contacts transformed through collider world isometry; apply_force now resets after each stepped update (rapier persistence documented); dead RigidBody::apply_impulse/apply_force component methods, PhysicsError, MovementConfig deleted; player_box(width,height) parameterized (demo 80x80 moved to callers); pixels_per_meter sanitized (0/NaN→100 + warn); CollisionPair tuple-compare (bit-packing overflow); raycast normalizes direction; velocity/position_iterations renamed solver/friction_iterations (rapier-accurate); unused toml/common/thiserror deps removed; physics_world.rs (977)→physics_world/{mod,bodies,stepping,queries,tests}, physics_system.rs (775)→physics_system/{mod,sync,update,tests} — all files ≤600 lines. Tests 47→58. AUDIO: per-play full-buffer clone eliminated (Arc<Vec<u8>>→Arc<[u8]>, Cursor direct to Decoder); master/sfx volume now re-applied to live sinks (base_volume tracked per ActiveSound); settings clamped at point of use; stop(handle) implemented (dead_code allow removed); PlaybackState deleted; IoError variant now actually used for file I/O; AudioResult exported; play_music_once() added; load_sound delegates to load_sound_from_bytes (DRY); false "crossfade"/"spatial audio" doc claims removed (ECS audio components documented as inspector-only, no runtime system). Tests 8→20. Both crates clippy-clean --all-targets. engine_core prelude updated (PlaybackState/MovementConfig/PhysicsError removed); README/training/AGENTS synced. Workspace: 902 passed / 0 failed / 34 ignored.
[2026-06-11] claude | DEBT-ENGINE-CORE | Resolved 5 of 6 engine_core TECH_DEBT Mediums (ARCH-006 behavior registry deferred to Phase 2+ as designed-Large). SRP-001: GlyphTextureCache extracted to glyph_texture_cache.rs (owns GlyphCacheKey→TextureHandle map; GameRunner calls prepare(commands, assets)/textures(); headless bookkeeping unit-tested, 4 tests; game.rs 594 lines). SRP-002: BehaviorRunner 145-line match → 7 private handlers + BehaviorCommands struct + apply_commands(), zero logic change. LOGIC-002: asset_manager unwrap → let-else with log::warn (engine_core now unwrap-free in production code). ARCH-007: ToastStyle struct (13 fields, Default == old hardcoded look, faded() alpha helper) on AchievementManager via set_toast_style()/toast_style(); reset() now logs save errors; 3 new tests incl. headless draw-command assertion. ARCH-003: all 15 lib.rs glob re-exports → explicit lists (prelude.rs untouched), verified against all downstream consumers. engine_core 161 tests; workspace 943/943, 0 ignored; clippy --workspace --all-targets clean. TECH_DEBT.md Medium 6→1.
[2026-06-11] claude | DEBT-ROLLUP | Coordinator pass after DEBT-ENGINE-CORE + DEBT-UI: fixed 4 pre-existing clippy warnings in renderer tests (sprite_data.rs: struct-init-with-Default ×2, iterator-zip instead of index loops) — clippy --workspace --all-targets now reports zero warnings. Synced root TECH_DEBT.md (engine_core 6→1 Medium, ui 3→1), PROJECT_ROADMAP.md Technical Debt section, AGENTS.md/CLAUDE.md test counts (936→943), engine_core CLAUDE.md file map. Remaining Mediums workspace-wide: engine_core ARCH-006, ui JUN-T1, input GAP-001, common ARCH-001/DRY-002, ecs_macros KISS-001.
[2026-06-11] claude | DEBT-UI | Resolved ui TECH_DEBT SRP-001 + SRP-002 (both Medium). SRP-002: context.rs (999 lines) split into context/ directory module (physics_world/ pattern) — mod.rs (227: UIContext struct, lifecycle, fonts, drawing primitives, custom-widget hooks), text.rs (204: label/measure family + shared text helpers, pub(super) for cross-file use), widgets.rs (341: button/slider/checkbox/float_input/panel/progress_bar), tests.rs (269: former inline tests, unchanged). SRP-001: font.rs (504 lines) split into font/ — mod.rs (236: FontManager facade, loading/storage, FontError/FontHandle/FontMetrics), glyph_cache.rs (218: GlyphCache struct owning RasterizedGlyph/GlyphInfo + bounded eviction, fills misses via fontdue), layout.rs (142: layout_text/measure_text fns owning TextLayout/LayoutGlyph). FontLoader deliberately NOT extracted (2 short methods writing to storage map — split would add indirection only). Public API identical: lib.rs re-exports untouched, all paths preserved. Arc<[u8]> bitmaps preserved. Empty leftover src/widgets/ dir removed. ui clippy-clean --all-targets, all files <600 lines, 70 ui tests (61 unit + 7 integration + 2 doc) unchanged. Verified against HEAD in isolated worktree: 936/936 workspace tests; live tree (with parallel engine_core work): 943/943, 0 ignored. TECH_DEBT.md Medium count 3→1 (JUN-T1 remains); CLAUDE.md file map + debt line updated.
[2026-06-12] claude | COLLIDER-OVERLAY | Collider visualization + adjustment in the editor (motivated by Pong colliders not lining up with sprites). NEW editor/src/collider_overlay.rs: collider_outline_segments() generates world-space outline segments mirroring rapier placement exactly (offset is body-local so it rotates with the body; Transform2D.scale ignored — collider sizes are absolute pixels); render_collider_overlay() draws them clipped to scene-view bounds via UIContext lines (Box=4 edges, Circle=32-seg polygon, CapsuleX/Y=2 arcs+2 sides). Colors are themed: 3 new EditorTheme tokens (collider_outline green, collider_sensor cyan, collider_selected yellow) + collider_overlay_colors() converter; selection wins over sensor. Toggle: EditorContext.show_colliders (default on) with is/set/toggle methods, C key shortcut (no Ctrl), View menu "Toggle Grid"(G)/"Toggle Colliders"(C) entries wired in menu_actions.rs. Inspector: edit_collider now edits shape dimensions (Box half-extents, Circle radius, capsule half-height/cap-radius) via new ranges::COLLIDER_EXTENT (0.5..=1000), distinct field_hints for undo merging — flows through existing SetColliderCommand. Known limitation: live rapier bodies are not rebuilt on component edit (PhysicsSystem sync only ADDS) — edits show in overlay immediately and apply on next body creation; flagged for follow-up. Workspace: 955/955 passing (+12), 0 ignored, clippy --all-targets clean. AGENTS.md/CLAUDE.md counts synced.
[2026-07-13] claude | GPP-AUDIT-BATCH-1 | First fix batch from PATTERNS_AUDIT.md (Game Programming Patterns audit; coordinator + 3 parallel agents, disjoint crates). GPP-01 (High, State, coordinator): BehaviorState bool soup (patrol_toward_b/is_chasing/is_waiting) replaced with StateMachine<BehaviorPhase> — new BehaviorPhase{Idle, Patrolling{toward}, Waiting{then_toward}, Chasing} + PatrolTarget{A,B} in ecs/behavior.rs; patrol/chase handlers in behavior_runner.rs rewritten as explicit FSM transitions (wait uses elapsed() clock; timer kept for jump cooldown); state_machine.rs gains first real consumer; +3 FSM regression tests (patrol arrive→wait→reverse, chase hysteresis, no-target idle). GPP-L5 (editor): CommandHistory undo_stack Vec→VecDeque, enforce_limit pop_front O(1); +oldest-dropped regression test. GPP-L6 (editor_integration): undo()/redo() return bool, mark_dirty gated — empty-stack Ctrl+Z no longer dirties a clean scene; +regression test. GPP-L3 (audio): SoundHandle process-global AtomicU32 → instance-local next_sound_id on AudioManager (SoundHandle::from_id, ids manager-local + deterministic). GPP-L4 (input): first-frame mouse warp fixed (has_position flag; 4 tests updated off the phantom (0,0)-baseline delta). GPP-17 (breakout): 10 tuning literals hoisted to constants.rs (grid impulses per effect, brick damage factors, launch spread, lost-ball pad); behavior bit-identical. Workspace: 1001/1001 passing (+6), 0 ignored, clippy --workspace --all-targets clean; breakout 43 + pong 5 green, both clippy-clean. Docs: resolved items moved TECH_DEBT→log_archive per live-docs convention; PATTERNS_AUDIT table marked; AGENTS/CLAUDE counts synced (995→1001).
[2026-07-13] claude | GPP-AUDIT-BATCH-2 | Physics event API batch from PATTERNS_AUDIT.md (single-agent, cross-crate). GPP-08 (Event Queue): game-facing collision API is now drain-style take_collision_events() -> Vec<CollisionData> on PhysicsSystem + PhysicsWorld — consumers own the Vec (no borrow held, .to_vec() footgun gone), second take in a frame returns empty (pinned by test); PhysicsSystem::collision_events() borrow accessor deleted; internal clear/append contract unchanged (PhysicsWorld::clear_collision_events still driver-owned). GPP-10 (Observer): synchronous collision callbacks DELETED (CollisionCallback type + 4 methods + update-loop invocation; were legacy, non-reentrant under &mut world, zero users outside physics' own tests) — delivery is now world event bus + take API only. GPP-L9 (Command): pending_velocities/pending_resets tuple-Vecs unified into pending_ops: Vec<(EntityId, DeferredBodyOp)> drained in call order ("reset then launch" ordering now structural, pinned by test). Migrated consumers: pong + breakout gameplay (one take per frame shared with Pickups::collect), breakout gameplay_tests, engine_core pickups test, physics ball_brick_bounce + delivery tests (callback-counter helper reworked to take-based counting; 3 pure callback-API tests deleted, 1 drain regression test added). Docs: physics/CLAUDE.md (architecture, update flow, event contract, key patterns, counts), root CLAUDE.md footgun (.to_vec() → take), training.md physics pattern, PROJECT_ROADMAP capability line, AGENTS.md counts. Workspace: 998/998 passing (physics 62→59), 0 ignored, clippy --workspace --all-targets clean; breakout 43 + pong 5 green, clippy-clean. TECH_DEBT: physics 0/3/6 → 0/1/5 (GPP-09 remains, rides ecs GPP-04); resolved items → log_archive.md.
[2026-07-13] claude | GPP-04-DIRTY-TRANSFORMS | Dirty-flagged transform hierarchy propagation (PATTERNS_AUDIT GPP-04 Medium + folded ecs SRP-003; design approved by Jesse: system-local VALUE-COMPARE cache, NOT store-level ECS dirty flags — value comparison catches every writer incl. WorldSnapshot::restore re-adds, identical physics writebacks stay clean, zero ECS API changes; decision of record: no shared change-tracking infra, GPP-09 = physics-local last-pushed cache, GPP-15 = coarse render counter). TransformHierarchySystem rewritten: HashMap<EntityId, CachedNode{local, parent, stamp}> baseline + reusable iterative DFS stack (TraversalFrame type alias) replaces root-loop + recursion — kills per-node children Vec allocs and entities()/get_root_entities() allocs; node dirty iff ancestor_dirty || baseline mismatch (local/parent) || global missing; clean frames do ZERO mutable World access; frame-stamp pruning evicts removed entities (stamps refreshed on every visit — incl. clean-under-dirty-ancestor path). New API: reset() (editor Stop calls it after snapshot.restore — belt-and-braces), recomputed_last_update()/visited_last_update()/tracked_entity_count() for headless observability. GlobalTransform2D now Copy + PartialEq. Tests: 9 new in ecs/tests/hierarchy_dirty.rs (clean-frame-zero, leaf-only=1, subtree-only=3, reparent, orphan+prune, identical-write-clean, disable/re-enable self-heal, global re-added, reset-full-recompute) + editor_integration test_stop_resets_transform_propagation_cache; 5 pre-existing propagation tests unchanged; clippy type_complexity fixed via named alias (no #[allow]). Workspace: 1008/1008 passing (+10: ecs 187, editor_integration 66), 0 ignored, clippy --workspace --all-targets clean. Docs: ecs CLAUDE.md (file map + GlobalTransform2D system-owned pitfall), TECH_DEBT moves → log_archive, PATTERNS_AUDIT GPP-04 ✅ + GPP-09/15 fix plans annotated with the no-shared-infra decision.
[2026-07-13] claude | GPP-09-EXTERNAL-EDITS | Physics external-edit detection (PATTERNS_AUDIT GPP-09 Medium; the GPP-04 no-shared-infra decision applied locally). PhysicsSystem keeps per-entity PushedState{position, rotation, collider} baselines: sync_entity_to_physics value-compares live entities' components against them — edited Transform2D → set_body_transform teleport (velocity preserved), edited Collider → rapier collider rebuild (add_collider removes-then-adds; sync_collider helper also covers add-later and component-removal → collider dropped), standalone-collider entities baselined too (shape edits rebuild; transform edits on body-less colliders remain out of scope, documented). sync_physics_to_ecs (&self → &mut self) refreshes baselines after writeback so rapier motion never reads as an edit; identical writes stay clean. Collider/ColliderShape gained PartialEq. Observability: external_edits_pushed_last_update(). Baselines pruned in destroy_entity/clear/orphan-GC. Kills TWO documented footguns: "sync only ADDS bodies" (root CLAUDE.md + physics/CLAUDE.md rewritten) and "editor collider edits don't reach the simulation". New Low EDIT-001: RigidBody config edits still need body recreation (velocity fields are writeback-owned). Tests: 5 new in physics/tests/external_edits.rs (teleport+velocity, writeback-not-an-edit ×10 frames, identical-write-clean, collider-enlarge → real collision event, collider-removal); physics_system/tests.rs kept <600 lines by moving them out. Workspace: 1013/1013 passing (+5, physics 64), 0 ignored, clippy --workspace --all-targets clean (map_entry warning fixed via entry().or_insert); breakout 43 + pong 5 green. Docs: PATTERNS_AUDIT GPP-09 ✅, TECH_DEBT physics 0 Mediums, log_archive entry, counts synced.
