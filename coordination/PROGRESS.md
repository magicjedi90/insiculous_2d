# Progress Log - Insiculous 2D

**Format:** `[YYYY-MM-DD HH:MM] AGENT-ID | TASK-XXX | Summary`

Agents: append entries here after pushing successfully. Most recent at top.

---

## Completed Tasks

[2026-08-19] claude | STUDIO-PIVOT + MENU-MOUSE | Studio premise pivot (adversarially reviewed: plan review-1 11 findings adjudicated, code reviews 2-3): Be Insiculous = AI dev studio; tiered AI-art rule (free web ships AI art, paid/marketplace never — DEION_STYLE.md §6 amended + README/CLAUDE/script header + 6 game CLAUDE.md + frogger README); roadmap reprioritized (Phase H + I1/I2 = open front ahead of F/G, H9 no longer gates on G, Phase I rewritten to real Cloudflare Workers setup, new Phase J "Insiculous Arcade" outline); TODO.md refreshed (H2-H8 ready-now queue); insiculous_web remote → milyramic (repo transferred; Mily's tree pulled — GitHub Actions deploys + beinsiculous.com already configured her side). MENU-MOUSE: MenuInput +NumpadEnter; MenuPanel row_rect/row_at/mouse_select (MenuMouse: hover-on-moved-frames selects, click confirms; headless tests); PauseMenu::update(+window_size) hit-tests its own panel (click executes rows, stray clicks ignored, mouse read only while paused); all 6 games adopted via shared panel-geometry constructors (input+drawing halves share layout) + click-to-dismiss info screens + hint sweep incl. pong/frogger en+pirate locales. Also fixed request-review.sh (kimi-code 0.36 CLI: -p arg, cd-scoped, 120KB argv guard) in engine+6 games+deion_assets variant, and adversarial-review.sh invoke(). Workspace 1262/1262, 0 ignored, clippy clean; games pong 11 / breakout 47 / snake 38 / invaders 36 / asteroids 42 / frogger 43, all clippy clean.

<!-- Example entries:
[2026-02-07 14:32] agent-01 | TASK-001 | Wired up viewport click-to-select in EditorGame::update(). 3 new tests, all 601 passing.
[2026-02-07 15:10] agent-02 | TASK-012 | Fixed double iteration in TransformHierarchySystem. Single-pass now. Added 5-level depth test. 599 passing.
-->
[2026-06-10] claude | EDITOR-AUDIT | Full DRY/SRP/KISS remediation of editor + editor_integration. Component dispatch unified into `editor_component_registry!` macro in stored_component.rs (was duplicated across 9+ sites; adding a component is now 1 line). Five *EditResult structs replaced by generic ComponentEdit<T> + apply_component_edit(); five Set*Commands collapsed into impl_set_component_command! macro. All 6 oversized files split (commands/, context/, editor_game/, panel_renderer/, field_style.rs); 28 hardcoded colors routed through EditorTheme (GizmoPalette, EditableFieldStyle tokens; menu/toolbar/hierarchy take &EditorTheme); magic numbers extracted (DEFAULT_SCENE_PATH, ranges mod, strides, constants.rs). MenuBar.render() split into 4 phases (layout pure + tested); gizmo render_axis_handle()/begin_drag_if() extracted. Clippy 0 warnings in both crates, 0 dead_code allows, 0 files >600 lines. TECH_DEBT.md: MAGIC-001/002/003, SRP-002, ARCH-001/002 resolved; editor_integration TECH_DEBT.md created. 879 workspace tests passing (editor 226, editor_integration 63).
[2026-06-11] claude | AUDIO-PHYSICS-AUDIT | Full DRY/SRP/KISS audit + remediation of audio and physics crates (2 parallel agents). PHYSICS: fixed 3 collision-event bugs (stale events re-emitted on no-step frames, sub-step event loss, contact points computed in collider-local space instead of world) — clearing moved out of step() into PhysicsSystem::update() via clear_collision_events(), step() now appends, contacts transformed through collider world isometry; apply_force now resets after each stepped update (rapier persistence documented); dead RigidBody::apply_impulse/apply_force component methods, PhysicsError, MovementConfig deleted; player_box(width,height) parameterized (demo 80x80 moved to callers); pixels_per_meter sanitized (0/NaN→100 + warn); CollisionPair tuple-compare (bit-packing overflow); raycast normalizes direction; velocity/position_iterations renamed solver/friction_iterations (rapier-accurate); unused toml/common/thiserror deps removed; physics_world.rs (977)→physics_world/{mod,bodies,stepping,queries,tests}, physics_system.rs (775)→physics_system/{mod,sync,update,tests} — all files ≤600 lines. Tests 47→58. AUDIO: per-play full-buffer clone eliminated (Arc<Vec<u8>>→Arc<[u8]>, Cursor direct to Decoder); master/sfx volume now re-applied to live sinks (base_volume tracked per ActiveSound); settings clamped at point of use; stop(handle) implemented (dead_code allow removed); PlaybackState deleted; IoError variant now actually used for file I/O; AudioResult exported; play_music_once() added; load_sound delegates to load_sound_from_bytes (DRY); false "crossfade"/"spatial audio" doc claims removed (ECS audio components documented as inspector-only, no runtime system). Tests 8→20. Both crates clippy-clean --all-targets. engine_core prelude updated (PlaybackState/MovementConfig/PhysicsError removed); README/training/AGENTS synced. Workspace: 902 passed / 0 failed / 34 ignored.
[2026-06-11] claude | DEBT-ENGINE-CORE | Resolved 5 of 6 engine_core TECH_DEBT Mediums (ARCH-006 behavior registry deferred to Phase 2+ as designed-Large). SRP-001: GlyphTextureCache extracted to glyph_texture_cache.rs (owns GlyphCacheKey→TextureHandle map; GameRunner calls prepare(commands, assets)/textures(); headless bookkeeping unit-tested, 4 tests; game.rs 594 lines). SRP-002: BehaviorRunner 145-line match → 7 private handlers + BehaviorCommands struct + apply_commands(), zero logic change. LOGIC-002: asset_manager unwrap → let-else with log::warn (engine_core now unwrap-free in production code). ARCH-007: ToastStyle struct (13 fields, Default == old hardcoded look, faded() alpha helper) on AchievementManager via set_toast_style()/toast_style(); reset() now logs save errors; 3 new tests incl. headless draw-command assertion. ARCH-003: all 15 lib.rs glob re-exports → explicit lists (prelude.rs untouched), verified against all downstream consumers. engine_core 161 tests; workspace 943/943, 0 ignored; clippy --workspace --all-targets clean. TECH_DEBT.md Medium 6→1.
[2026-06-11] claude | DEBT-ROLLUP | Coordinator pass after DEBT-ENGINE-CORE + DEBT-UI: fixed 4 pre-existing clippy warnings in renderer tests (sprite_data.rs: struct-init-with-Default ×2, iterator-zip instead of index loops) — clippy --workspace --all-targets now reports zero warnings. Synced root TECH_DEBT.md (engine_core 6→1 Medium, ui 3→1), PROJECT_ROADMAP.md Technical Debt section, AGENTS.md/CLAUDE.md test counts (936→943), engine_core CLAUDE.md file map. Remaining Mediums workspace-wide: engine_core ARCH-006, ui JUN-T1, input GAP-001, common ARCH-001/DRY-002, ecs_macros KISS-001.
[2026-06-11] claude | DEBT-UI | Resolved ui TECH_DEBT SRP-001 + SRP-002 (both Medium). SRP-002: context.rs (999 lines) split into context/ directory module (physics_world/ pattern) — mod.rs (227: UIContext struct, lifecycle, fonts, drawing primitives, custom-widget hooks), text.rs (204: label/measure family + shared text helpers, pub(super) for cross-file use), widgets.rs (341: button/slider/checkbox/float_input/panel/progress_bar), tests.rs (269: former inline tests, unchanged). SRP-001: font.rs (504 lines) split into font/ — mod.rs (236: FontManager facade, loading/storage, FontError/FontHandle/FontMetrics), glyph_cache.rs (218: GlyphCache struct owning RasterizedGlyph/GlyphInfo + bounded eviction, fills misses via fontdue), layout.rs (142: layout_text/measure_text fns owning TextLayout/LayoutGlyph). FontLoader deliberately NOT extracted (2 short methods writing to storage map — split would add indirection only). Public API identical: lib.rs re-exports untouched, all paths preserved. Arc<[u8]> bitmaps preserved. Empty leftover src/widgets/ dir removed. ui clippy-clean --all-targets, all files <600 lines, 70 ui tests (61 unit + 7 integration + 2 doc) unchanged. Verified against HEAD in isolated worktree: 936/936 workspace tests; live tree (with parallel engine_core work): 943/943, 0 ignored. TECH_DEBT.md Medium count 3→1 (JUN-T1 remains); CLAUDE.md file map + debt line updated.
[2026-06-12] claude | COLLIDER-OVERLAY | Collider visualization + adjustment in the editor (motivated by Pong colliders not lining up with sprites). NEW editor/src/collider_overlay.rs: collider_outline_segments() generates world-space outline segments mirroring rapier placement exactly (offset is body-local so it rotates with the body; Transform2D.scale ignored — collider sizes are absolute pixels); render_collider_overlay() draws them clipped to scene-view bounds via UIContext lines (Box=4 edges, Circle=32-seg polygon, CapsuleX/Y=2 arcs+2 sides). Colors are themed: 3 new EditorTheme tokens (collider_outline green, collider_sensor cyan, collider_selected yellow) + collider_overlay_colors() converter; selection wins over sensor. Toggle: EditorContext.show_colliders (default on) with is/set/toggle methods, C key shortcut (no Ctrl), View menu "Toggle Grid"(G)/"Toggle Colliders"(C) entries wired in menu_actions.rs. Inspector: edit_collider now edits shape dimensions (Box half-extents, Circle radius, capsule half-height/cap-radius) via new ranges::COLLIDER_EXTENT (0.5..=1000), distinct field_hints for undo merging — flows through existing SetColliderCommand. Known limitation: live rapier bodies are not rebuilt on component edit (PhysicsSystem sync only ADDS) — edits show in overlay immediately and apply on next body creation; flagged for follow-up. Workspace: 955/955 passing (+12), 0 ignored, clippy --all-targets clean. AGENTS.md/CLAUDE.md counts synced.
[2026-07-13] claude | GPP-AUDIT-BATCH-1 | First fix batch from PATTERNS_AUDIT.md (Game Programming Patterns audit; coordinator + 3 parallel agents, disjoint crates). GPP-01 (High, State, coordinator): BehaviorState bool soup (patrol_toward_b/is_chasing/is_waiting) replaced with StateMachine<BehaviorPhase> — new BehaviorPhase{Idle, Patrolling{toward}, Waiting{then_toward}, Chasing} + PatrolTarget{A,B} in ecs/behavior.rs; patrol/chase handlers in behavior_runner.rs rewritten as explicit FSM transitions (wait uses elapsed() clock; timer kept for jump cooldown); state_machine.rs gains first real consumer; +3 FSM regression tests (patrol arrive→wait→reverse, chase hysteresis, no-target idle). GPP-L5 (editor): CommandHistory undo_stack Vec→VecDeque, enforce_limit pop_front O(1); +oldest-dropped regression test. GPP-L6 (editor_integration): undo()/redo() return bool, mark_dirty gated — empty-stack Ctrl+Z no longer dirties a clean scene; +regression test. GPP-L3 (audio): SoundHandle process-global AtomicU32 → instance-local next_sound_id on AudioManager (SoundHandle::from_id, ids manager-local + deterministic). GPP-L4 (input): first-frame mouse warp fixed (has_position flag; 4 tests updated off the phantom (0,0)-baseline delta). GPP-17 (breakout): 10 tuning literals hoisted to constants.rs (grid impulses per effect, brick damage factors, launch spread, lost-ball pad); behavior bit-identical. Workspace: 1001/1001 passing (+6), 0 ignored, clippy --workspace --all-targets clean; breakout 43 + pong 5 green, both clippy-clean. Docs: resolved items moved TECH_DEBT→log_archive per live-docs convention; PATTERNS_AUDIT table marked; AGENTS/CLAUDE counts synced (995→1001).
[2026-07-13] claude | GPP-AUDIT-BATCH-2 | Physics event API batch from PATTERNS_AUDIT.md (single-agent, cross-crate). GPP-08 (Event Queue): game-facing collision API is now drain-style take_collision_events() -> Vec<CollisionData> on PhysicsSystem + PhysicsWorld — consumers own the Vec (no borrow held, .to_vec() footgun gone), second take in a frame returns empty (pinned by test); PhysicsSystem::collision_events() borrow accessor deleted; internal clear/append contract unchanged (PhysicsWorld::clear_collision_events still driver-owned). GPP-10 (Observer): synchronous collision callbacks DELETED (CollisionCallback type + 4 methods + update-loop invocation; were legacy, non-reentrant under &mut world, zero users outside physics' own tests) — delivery is now world event bus + take API only. GPP-L9 (Command): pending_velocities/pending_resets tuple-Vecs unified into pending_ops: Vec<(EntityId, DeferredBodyOp)> drained in call order ("reset then launch" ordering now structural, pinned by test). Migrated consumers: pong + breakout gameplay (one take per frame shared with Pickups::collect), breakout gameplay_tests, engine_core pickups test, physics ball_brick_bounce + delivery tests (callback-counter helper reworked to take-based counting; 3 pure callback-API tests deleted, 1 drain regression test added). Docs: physics/CLAUDE.md (architecture, update flow, event contract, key patterns, counts), root CLAUDE.md footgun (.to_vec() → take), training.md physics pattern, PROJECT_ROADMAP capability line, AGENTS.md counts. Workspace: 998/998 passing (physics 62→59), 0 ignored, clippy --workspace --all-targets clean; breakout 43 + pong 5 green, clippy-clean. TECH_DEBT: physics 0/3/6 → 0/1/5 (GPP-09 remains, rides ecs GPP-04); resolved items → log_archive.md.
[2026-07-13] claude | GPP-04-DIRTY-TRANSFORMS | Dirty-flagged transform hierarchy propagation (PATTERNS_AUDIT GPP-04 Medium + folded ecs SRP-003; design approved by Jesse: system-local VALUE-COMPARE cache, NOT store-level ECS dirty flags — value comparison catches every writer incl. WorldSnapshot::restore re-adds, identical physics writebacks stay clean, zero ECS API changes; decision of record: no shared change-tracking infra, GPP-09 = physics-local last-pushed cache, GPP-15 = coarse render counter). TransformHierarchySystem rewritten: HashMap<EntityId, CachedNode{local, parent, stamp}> baseline + reusable iterative DFS stack (TraversalFrame type alias) replaces root-loop + recursion — kills per-node children Vec allocs and entities()/get_root_entities() allocs; node dirty iff ancestor_dirty || baseline mismatch (local/parent) || global missing; clean frames do ZERO mutable World access; frame-stamp pruning evicts removed entities (stamps refreshed on every visit — incl. clean-under-dirty-ancestor path). New API: reset() (editor Stop calls it after snapshot.restore — belt-and-braces), recomputed_last_update()/visited_last_update()/tracked_entity_count() for headless observability. GlobalTransform2D now Copy + PartialEq. Tests: 9 new in ecs/tests/hierarchy_dirty.rs (clean-frame-zero, leaf-only=1, subtree-only=3, reparent, orphan+prune, identical-write-clean, disable/re-enable self-heal, global re-added, reset-full-recompute) + editor_integration test_stop_resets_transform_propagation_cache; 5 pre-existing propagation tests unchanged; clippy type_complexity fixed via named alias (no #[allow]). Workspace: 1008/1008 passing (+10: ecs 187, editor_integration 66), 0 ignored, clippy --workspace --all-targets clean. Docs: ecs CLAUDE.md (file map + GlobalTransform2D system-owned pitfall), TECH_DEBT moves → log_archive, PATTERNS_AUDIT GPP-04 ✅ + GPP-09/15 fix plans annotated with the no-shared-infra decision.
[2026-07-13] claude | GPP-09-EXTERNAL-EDITS | Physics external-edit detection (PATTERNS_AUDIT GPP-09 Medium; the GPP-04 no-shared-infra decision applied locally). PhysicsSystem keeps per-entity PushedState{position, rotation, collider} baselines: sync_entity_to_physics value-compares live entities' components against them — edited Transform2D → set_body_transform teleport (velocity preserved), edited Collider → rapier collider rebuild (add_collider removes-then-adds; sync_collider helper also covers add-later and component-removal → collider dropped), standalone-collider entities baselined too (shape edits rebuild; transform edits on body-less colliders remain out of scope, documented). sync_physics_to_ecs (&self → &mut self) refreshes baselines after writeback so rapier motion never reads as an edit; identical writes stay clean. Collider/ColliderShape gained PartialEq. Observability: external_edits_pushed_last_update(). Baselines pruned in destroy_entity/clear/orphan-GC. Kills TWO documented footguns: "sync only ADDS bodies" (root CLAUDE.md + physics/CLAUDE.md rewritten) and "editor collider edits don't reach the simulation". New Low EDIT-001: RigidBody config edits still need body recreation (velocity fields are writeback-owned). Tests: 5 new in physics/tests/external_edits.rs (teleport+velocity, writeback-not-an-edit ×10 frames, identical-write-clean, collider-enlarge → real collision event, collider-removal); physics_system/tests.rs kept <600 lines by moving them out. Workspace: 1013/1013 passing (+5, physics 64), 0 ignored, clippy --workspace --all-targets clean (map_entry warning fixed via entry().or_insert); breakout 43 + pong 5 green. Docs: PATTERNS_AUDIT GPP-09 ✅, TECH_DEBT physics 0 Mediums, log_archive entry, counts synced.
[2026-07-13] claude | GPP-15-UPLOAD-GATE | Static-scene sprite upload skip (PATTERNS_AUDIT GPP-15 Medium + folded renderer ARCH-007; per the GPP-04 decision: render-path-local change detection, no ECS hooks). NEW renderer sprite/instance_cache.rs InstanceCache (CPU-only, headless-testable): stage(batches) flattens into a persistent staging buffer, byte-compares (bytemuck cast, NaN-safe/exact) flattened instances AND batch layout (texture, count) against the last-uploaded snapshot, swap-on-change; uploads_performed/uploads_skipped counters. SpritePipeline::prepare_sprites now gates write_buffer on stage() — static scenes re-render from the GPU buffer already uploaded; detection is on BUILT data (after game.render()), so procedural render code stays sound and static UI frames skip too. ARCH-007's per-frame scratch Vec eliminated (persistent staging). engine_core: GameRunner owns persistent game_batcher/ui_batcher (clear() retains capacity — no per-frame SpriteBatcher::new()); per-frame clone of every batch removed (sort_batch_refs orders &SpriteBatch refs, filters now-empty batches from persistent batchers, preserves game-then-UI painter order). Tests: 4 new InstanceCache unit tests (identical-skip+counters, moved-instance re-upload, same-bytes-different-layout re-upload, empty<->content). Workspace: 1017/1017 passing (+4, renderer 74), 0 ignored, clippy --workspace --all-targets clean; breakout 43 + pong 5 green. Docs: PATTERNS_AUDIT GPP-15 ✅, renderer TECH_DEBT 0 Mediums (ARCH-007 → log_archive), CLAUDE.md arch diagram + counts synced.
[2026-07-13] claude | GPP-13-REGISTRY-INSPECTOR | Registry-generated editable inspector (PATTERNS_AUDIT GPP-13 Medium). editor_component_registry! entries now carry an edit spec — `{ edit edit_sprite => SetSpriteCommand }` (field editing) or `{ readonly }` (serde display + remove header) — and the macro generates edit_all_components(): per-component field editors, undo-recorded writeback via apply_component_edit (try_merge_or_push/field_hint semantics preserved), remove [X] buttons + RemoveComponentCommand execution, in registry order; new registry_edit_block! helper macro expands the three block shapes (@fixed editable, @removable editable, @removable readonly). editor_integration panel_renderer/inspector.rs: ~11 hand-written blocks deleted (357 → 160 lines), now a thin shell (readonly-during-play branch + add-component popup calling the generated fn). apply_component_edit + remove_button moved editor_integration → editor/src/component_editors.rs (kept stored_component.rs at 568 lines, under the 600 rule); lib.rs exports updated; editor_integration writeback tests re-pointed to editor::apply_component_edit (all 10 pass unchanged). /add-component skill Steps 4–5 rewritten: new component = one registry line (+ edit_* fn + impl_set_component_command! only if editable); root CLAUDE.md single-sources table updated (writeback row). New editor test: test_edit_all_components_covers_present_components_and_advances_y (count/y/no-spurious-undo). Workspace: 1018/1018 passing (+1, editor 252, editor_integration 66), 0 ignored, clippy --workspace --all-targets clean. editor_integration at 0 Mediums; GPP audit now 13 findings resolved.
[2026-07-13] claude | GPP-14-STABLE-UNDO-IDS | Stable EntityIds across undo/redo (PATTERNS_AUDIT GPP-14 Medium). Instead of the audit's fallback (selection remapping), went straight to the Godot-style root fix: CreateEntityCommand::execute (redo) and DeleteEntityCommand::undo now resurrect the entity via world.create_entity_with_id(self.entity) — ids are never recycled so the slot is guaranteed free — killing the entire staleness class (Selection AND any history commands referencing the id stay valid across undo/redo cycles; no remap machinery). BONUS pre-existing bug found by the new tests and fixed: CreateEntityCommand pushed via push_already_executed never cleared its `captured` flag (execute() is never called first), so the FIRST redo after an undo silently recreated nothing — undo() now clears the flag (any execute after undo is a redo). 3 new regression tests: delete→undo resurrects same id, create→undo→redo resurrects same id, and the money shot — a SetTransformCommand recorded BEFORE a delete still resolves after the delete is undone (previously silently no-opped against a dead id). Workspace: 1021/1021 passing (+3, editor 255), 0 ignored, clippy --workspace --all-targets clean. editor at 0 Mediums (only GPP-L7 doc note left). GPP audit: 14 of 17 numbered findings resolved.
[2026-07-13] claude | GPP-07-RUNTIME-PREFABS | Runtime prefab spawning (PATTERNS_AUDIT GPP-07 Medium + folded DRY-010). SceneInstance now RETAINS the scene's prefab table (pub prefabs field + has_prefab()) and offers spawn_prefab(world, assets, name, overrides) -> Result<EntityId> — the Prototype pattern's actual purpose: stamp out balls/enemies/drops mid-game from RON-defined templates, with scene-file override semantics (reuses merge_components + add_component_to_entity); a spawn that fails mid-build removes the half-built entity (no debris); spawned entities are caller-owned (not added to instance bookkeeping). NEW TextureResolver trait in texture_ref.rs (resolve_texture(&mut self, ref)) — the GPU seam: AssetManager is the production impl, tests stub it; SceneLoader::instantiate/load_and_instantiate/internals generified from &mut AssetManager to &mut impl TextureResolver (call sites unchanged — AssetManager implements the trait; breakout compiles + tests green untouched). DRY-010 folded: merge_components' duplicated replace-or-append loop extracted to apply_component_layer(). Dead private SceneLoader::resolve_texture wrapper deleted. Tests: 5 new headless integration tests in engine_core/tests/prefab_spawning.rs (prefab table retained, spawn stamps new entity w/ components + scene bookkeeping untouched, overrides replace, unknown prefab errors w/o debris, mid-build failure removes half-built entity via FailingResolver). Workspace: 1026/1026 passing (+5, engine_core 186), 0 ignored, clippy --workspace --all-targets clean; breakout 43 + pong 5 green. engine_core Mediums 3→2 (ARCH-006, GPP-03 remain). GPP audit: 15 of 17 numbered findings resolved.
[2026-07-13] claude | GPP-03-PART1-PROMOTIONS + AUDIT-CLOSED | Game-agnostic promotions (promotion #2 under the standing directive; the pre-game-3 item). Five pieces of pong↔breakout copy-paste moved into the engine, both games refactored onto each (tests stay green — the sustainability proof): (1) engine_core::ChaosTheme (new chaos_theme.rs, in prelude) — per-mode presentation tokens with the shared neon palette as for_mode() defaults; fields de-genre'd (ball_color→accent_color, wall_color→structure_color); breakout keeps a thin theme_for() with its 3 Normal-mode overrides, pong uses defaults directly. (2) engine_core::grid::step_and_emit_grid(grid, world, lines, dt, debug_colliders) — per-frame grid driver + F1 magenta collider overlay; narrow params (not GameContext) so it's headless-testable. (3) ecs::sprite_components::set_sprites_visible(world, entities, visible) — menu-visibility loop (games keep entity lists + state matching). (4) common::{hash_u32, hash_f32} (new hash.rs, prelude re-export). (5) engine_core::game_root!() macro + assets::game_root_from() — exe-dir→manifest-dir anchoring; macro so the GAME crate's CARGO_MANIFEST_DIR bakes in. Engine tests for every piece (+10: common 29, engine_core 193, ecs 188). ALSO: PATTERNS_AUDIT.md RETIRED per Jesse — audit closed at 15/17 numbered findings resolved; closing summary added to log_archive.md; all open GPP items re-homed self-contained in live docs (games/TECH_DEBT.md carries GPP-03 part 2 with full candidate list + GPP-11/12; GPP-06/16 in engine_core/ecs TECH_DEBT; GPP-02 decision-of-record in ecs); every "see PATTERNS_AUDIT.md" reference scrubbed from AGENTS/training/PROJECT_ROADMAP/TECH_DEBT files; roadmap stale 995 count fixed. Workspace: 1036/1036 passing (+10), 0 ignored, clippy --workspace --all-targets clean; breakout 43 + pong 5 green, clippy-clean.
[2026-07-13] claude | LIFETIME + SI-PREP | Roadmap Phase B Gap 2 shipped: ecs::lifetime — Lifetime{remaining} component + LifetimeSystem (prelude-exported; ticks carriers down, remove_entity on expiry — hierarchy auto-detaches, physics GC reclaims rapier state) with 4 headless tests incl. the roadmap acceptance criterion (alive t=0.4 / gone t=0.6 for 0.5s). Unblocks bullets/effects for games 3/8/11/16. ecs TECH_DEBT gained the sparse-set-first note on the GPP-02 storage item (decision: when the trigger fires, evaluate dense-Vec sparse sets BEFORE full archetypes — editor add/remove churn punishes migration). /new-game skill refreshed for this week's engine (game_root!(), engine ChaosTheme w/ struct-update overrides, step_and_emit_grid, take_collision_events, hash_f32 prelude, Lifetime, spawn_prefab, set_sprites_visible, + rule-of-three check section pointing at games/TECH_DEBT GPP-03 part 2). coordination/TODO.md rewritten as a live queue: TASK-SI-001 (Space Invaders, with build notes) on top; the fossil Phase-1 task list (all shipped) retired; debt intentionally not duplicated (lives in TECH_DEBT docs). Workspace: 1040/1040 passing (+4, ecs 192), 0 ignored, clippy clean.
[2026-07-13] claude | TASK-SI-001 | Space Invaders (game 3) shipped in ../games/space_invaders/ + GPP-03 part 2 rule-of-three CLOSED. GAME: 5×10 kinematic invader fleet marching as one shared offset (per-frame set_kinematic_target to home-slot+offset; march_speed ramps MARCH_SPEED_BASE→MAX as the fleet thins; edge bounce clamps offset + descends DESCEND_STEP), player cannon (kinematic, mouse/keys), bullets = dynamic sensors carrying engine Lifetime (LifetimeSystem despawns strays; off-screen cull is the primary path), 4 destructible barrier bunkers (grids of static sensor blocks), ONE take_collision_events() drain shared by all consumers. Game-side AABB (rects_overlap) for the pairs rapier never reports: kinematic-invader-vs-static-block chomping, kinematic-vs-kinematic invader-vs-player, and fast bullet-vs-bullet cancels (closing-speed-inflated). Chaos: Insane = 1.8× march + 2.4× fire rate; Ridiculous = twin cannons + stacked volleys (6-bullet cap vs classic 1); Insiculous = both. 10 achievements (4 clear + 4 perfect + sharpshooter 10-kill streak + last stand) with DISPLAY_SECTIONS coverage test. 25 tests incl. 4 physics sims proving every collision pair the game depends on (bullet-vs-invader, bullet-vs-player, bullet-vs-barrier-sensor, Lifetime expiry); clippy --all-targets clean; --features editor compiles. PROMOTIONS (third literal duplication confirmed → engine, pong+breakout refactored on): engine_core::menu_input::MenuInput (pong's struct == breakout's nav_keys/menu_navigate; 5 tests), engine_core::spawn_helpers::spawn_background(world, tex, color, window_size), grid::default_playfield_grid(theme) (was build_grid verbatim in both games), and RENDER_UNIT=80.0 at the engine_core crate root (game.rs render path now uses it; games' local consts deleted) — all in prelude. NOT promoted (no third use, per the rule): paddle/ball/wall spawner shapes, particle preset semantics, Serving flow skeleton. Breakout 43→41 (menu tests moved to engine), pong 5, SI 25. Docs: PROJECT_ROADMAP game 3 ☑ (+GPP-03 refs), AGENTS/training/engine_core CLAUDE.md/TECH_DEBT rollup + engine_core+games TECH_DEBT synced, log_archive Game 3 entry, /new-game skill refreshed (most-recent=space_invaders, MenuInput/default_playfield_grid/spawn_background/RENDER_UNIT conventions, kinematic-pair footgun + sim-test rule). Workspace: 1048/1048 passing (+8: engine_core 201), 0 ignored, clippy --workspace --all-targets clean.
[2026-07-13] claude | TASK-SNAKE-001 | Snake (game 4 of the 20 Games Challenge) shipped in ../games/snake/ (own git repo, initial commit 4cd1531). First game with NO physics — segments/food/walls are sprite-only entities (Name+Transform2D+Sprite); all rules are pure functions: step_snake (VecDeque<IVec2> head-first, vacating-tail rule, wall vs wrap), next_direction (cap-2 turn buffer, 180° reversals rejected at apply time), place_food (hash_u32 → linear probe, deterministic), tick_interval/food_count/walls_wrap (chaos single-sources). Fixed-tick accumulator drives movement (first discrete-step game). Chaos: Insane = 0.10s base tick shrinking 2ms/food to 0.05s floor; Ridiculous = wrap-around walls (dimmed wall frame) + 2 pellets; Insiculous = both. 9 achievements (3 length milestones, 4 per-mode feasts @15, Ouroboros, Quick Snack) with DISPLAY_SECTIONS coverage test. Engine neon look via promoted helpers (ChaosTheme, default_playfield_grid + step_and_emit_grid, spawn_background, MenuInput, set_sprites_visible, game_root!). 31 tests green headless, clippy --all-targets 0 warnings, editor feature compiles. NO engine changes (rule-of-three: tick accumulator is first-use, stays game-side). Docs: roadmap Game 4 ☑, AGENTS.md counts/priority, log_archive.md entry, TODO.md → TASK-ASTEROIDS-001 next.
[2026-07-13] claude | TASK-ASTEROIDS-001 | Asteroids (game 5 of the 20 Games Challenge) shipped in ../games/asteroids/ (own git repo, initial commit c4f6a00) — PHASE A COMPLETE. First wireframe-vector game: ship (4-segment neon hull) and rocks (10-vertex hash-seeded irregular polygons, generated once at spawn, stored on the Asteroid struct) drawn by pushing LineVertex pairs into ctx.lines after step_and_emit_grid; bullets/particles/background stay sprites. Physics: PhysicsConfig::space() (zero-g); ship = dynamic sensor with free rotation driven by ONE set_velocity(linear, angular) per frame from pure integrate_ship (thrust along forward(), game-owned drag, max-speed clamp — no game-side heading/velocity state, rapier's Transform2D writeback is the source of truth); rocks = solid dynamic circles (restitution 1.0, they elastically bounce off each other); bullets = dynamic sensors + Lifetime (SI recipe). Screen wrap = pure wrap_position + Transform2D.position writes on live bodies (GPP-09 teleport, velocity preserved — proven by a dedicated sim test). Ship-vs-rock death = game-side circle test (not events: a rock overlapping through i-frames must kill when mercy ends; stale started events can't). Splitting: split_spec (Large→2 Medium, Medium→2 Small; Ridiculous family splits Large 3-ways), children offset 1.2×parent-radius along diverging hash-fanned velocities. Waves endless (base 4 +1/wave cap 9, +1 Ridiculous), border spawns with deterministic opposite-side fallback (≥600px guarantee proven in test). Chaos: Insane = rock speed ×1.6 + i-frames halved; Ridiculous = triple splits + bigger waves; Insiculous = both. 11 achievements (4 wave-5 per mode, 2 score tiers, sharpshooter/close-call/untouchable/double-tap/living-on-edge) with DISPLAY_SECTIONS coverage test; extra life every 10k. 37 tests green headless incl. 6 physics sims (bullet-vs-dynamic-rock, rock-rock bounce, Lifetime expiry, GPP-09 wrap proof, wave spawn safe-dist, real-body thrust); clippy --all-targets clean; --features editor compiles; all files <600 lines. NO engine changes (rule-of-three: screen wrap + wireframe emission are first-use, stay game-side; promote if game 6+ needs them). Docs: roadmap Phase A ☑ + Game 5 ☑, AGENTS.md counts/priority (next: Phase B Gap 1 CameraFollow), training.md game lists, TODO.md → TASK-GAP-CAMERA-001.
[2026-07-16] claude | TASK-GAP-CAMERA-001 (Phase B Gaps 1+3) | PHASE B COMPLETE. Gap 1: Behavior::CameraFollow (8th variant; dt-corrected exponential lerp, per-frame fraction calibrated at 60 FPS, clamp-to-edge dead zone, offset; new `positions` channel on BehaviorCommands) + RenderManager::sync_main_camera (main-camera entity's Transform2D drives the render camera before game.render(); worlds without one untouched) + editor variant arm (ranges::FRACTION; dead_zone read-only pending Option widget). Gap 3: ecs::Tilemap (crates/ecs/src/tilemap.rs — row-major tile grid, 0=empty, top-left-tile anchor, rows grow down, tile_size in pixels, depth default -1.0; sprite_instances() → plain TileInstance data) + engine_core/src/tilemap_render.rs expansion at top of default Game::render (one batch per map, InstanceCache gates static uploads) + full wiring (ECS registry, scene RON Tilemap variant w/ texture-ref tileset, editor registry readonly/Rendering). 21 new tests. Workspace: 1069/1069 passed, 0 ignored, clippy --all-targets clean. Commits 8744aba, 0e44a9f. Next: game 6 Frogger.
[2026-07-16] claude | 2-PLAYER + UNIVERSAL INPUT SETTINGS | Universal player-aware input layer shipped + ALL 5 games made 2-player (Jesse's decisions: Breakout co-op shared field, Snake versus, Invaders/Asteroids co-op, P1=kb/mouse+pad0 P2=pad1+arrows, engine API + saved bindings). ENGINE: input crate gained axis edge detection (prev-frame snapshot; InputSource::GamepadAxis(id, axis, AxisDirection), AXIS_ACTIVATION_THRESHOLD=0.5), GamepadConnected/Disconnected events, and player.rs — InputSettings/PlayerBindings/PlayerId with device-relative PlayerSource (one pad id per player; assign_pad never rewrites bindings), is_active/just_activated[_any], move_x/move_y (digital merged w/ stick, clamped, +y=up; axis-threshold bindings excluded from digital half so half-stick reads 0.5). engine_core: input_settings_io.rs (versioned Vec-of-entries JSON; missing→defaults WRITTEN for hand-editing; corrupt→warn+defaults; save on CloseRequested), GameConfig.input_settings_path, GameContext.players (&mut InputSettings), MenuInput scans EVERY pad (dpad/stick-edge nav, A/Start confirm, B back — all game menus controller-ready with zero game edits), BehaviorRunner jump raw-Space→GameAction::Action1, game.rs split (render tail → child module game/render.rs; 530 lines). GAP-004 CLOSED → log_archive; GAP-001 reduced to just the gilrs poll — BLOCKED on libudev-dev (design of record in input TECH_DEBT + memory). GAMES (agents: asteroids-2p/invaders-2p/snake-2p; pong+breakout by coordinator, all verified): pong migrated (paddle_dy(axis) analog, 8 tests); breakout co-op — top paddle PADDLE_TOP_Y w/ downward bounce (paddle_bounce_direction_for), rebuild_playfield per mode (co-op destroys top wall for a top sensor), serve flips to losing edge, both paddles catch pickups, LEVELS_2P roster + 4 new level{1-4}_2p.scene.ron middle-band scenes, gameplay.rs split to gameplay/{mod,paddles,balls,bricks,flow}, levels tests → levels_tests.rs (47 tests); snake versus — SnakeState vec, pure resolve_versus_step (pre-step heads, vacating-tail rule, head-on=Draw), GameResult::{Solo,Winner,Draw}, gameplay/{mod,rules} split (38 tests); asteroids co-op — ShipState vec, per-ship lives/score/respawn, despawn-at-0 survivor-plays-on, coop_over, spawn_position avoid-all-ships, structural no-friendly-fire (42 tests); invaders co-op — PlayerState vec, new gameplay/players.rs, per-player fire caps/streaks, dead cannon not an invasion target, coop_defeated (36 tests). All games: 3-item title menus (1P/2P/Achievements), just_activated_any(Menu/Action1) flow input, F1 stays raw, 1P merges P1+P2, input_settings_path wired. Docs: AGENTS/training/PROJECT_ROADMAP/input+engine_core CLAUDE.md+TECH_DEBT synced; memory two-player-and-input-settings.md. Workspace: 1093/1093 passed, 0 ignored, clippy --workspace --all-targets clean; games 8/47/38/42/36 all clippy-clean.
[2026-07-16] claude | PAUSE + MENU CHROME | Universal pause + distinguished menu windows shipped engine-wide and in all 5 games (Jesse's asks: pause is a must; menus shouldn't be floating text). ENGINE: GameContext.time_scale (read-write like chaos_mode; scales ONLY engine-side particle stepping — paused games freeze bursts too); engine_core/pause.rs PauseMenu/PauseAction (Menu=Esc/any-pad-Start toggles; Resume/Restart/Quit-to-Title; Esc/Start/B always resume — toggle checked BEFORE confirm so Start can't execute the highlighted item; Resumed = skip-this-frame so the keypress can't leak into gameplay; takes &InputSettings+&InputHandler NOT GameContext — headless-testable, 6 tests); engine_core/menu_panel.rs MenuPanel/MenuStyle (opaque theme-derived window: MenuStyle::from_theme, border, accent separator, corner ticks, ▶-cursor highlight-bar rows, item/item_colored/line/hint, pure panel_rect tested, draw_as_overlay = input-blocking scrim used by pause). Font fact: shared font.ttf HAS ▶▲▼■● but NOT ═─░█ (raw cmap parse; identical md5 across games) — flair is rect-based, cursor is ▶. GAMES (pong+breakout by coordinator; snake-pause/asteroids-pause/invaders-pause agents, all verified): pause gate at top of gameplay update (pausable states only: pong/breakout Serving|Playing, others Playing; GameOver keeps direct Menu→title — instant Esc-quit REMOVED from play states, quit lives in the pause menu), ctx.time_scale = pause.time_scale(), paused branch RE-EMITS the grid via step_and_emit_grid(dt=0) (ctx.lines clears every frame — frozen scene stays visible; asteroids also re-emits wireframes); pause overlay at end of gameplay draw; MenuPanel chrome on every title/select/achievements screen + game-over panels (breakout level select keeps per-mode banner colors via item_colored); serve hints now say "ESC to pause". Docs: AGENTS/training (Pause Pattern + Menu Chrome Pattern) /PROJECT_ROADMAP (capabilities row)/engine_core CLAUDE.md; memory updated. Workspace: 1109/1109 passed (engine_core 238), 0 ignored, clippy --all-targets clean; games 8/47/38/42/36 all clippy-clean.
[2026-07-16] claude | EXIT BUTTON | Exit from pause menu + every title screen (Jesse's ask). ENGINE: GameContext.exit_requested (read-write flag; games write true → GameRunner runs the SAME clean shutdown as window close — on_exit, input-settings save, scene stop/shutdown, event_loop.exit — via a new shared shutdown() helper checked in about_to_wait after update). PauseAction::ExitGame + "Exit Game" as the pause menu's 4th item (tests extended: confirm table + wrap-to-last now proves ExitGame). GAMES ×5: pause gate gained the ExitGame→ctx.exit_requested arm; title menus are now 4 items (…/Achievements/Exit — navigate 4, arm 2 = Achievements explicit, _ = exit; 4-row title panels). Verified: workspace 1109/1109 (0 ignored) + clippy --all-targets clean; games 8/47/38/42/36 all clippy-clean.
[2026-07-16] claude | PANELS + DATA-DRIVEN UI + LOCALIZATION | Executed Jesse's approved 4-phase plan (~/.claude/plans/the-editor-feels-really-fizzy-blanket.md). PHASE A (panels): dock.rs split → dock/{mod,render,tests}; DockPanel.collapsed — edge panels collapse to a HEADER_HEIGHT strip via line-primitive chevrons (size preserved); resize clamped [min_size, half-dock] w/ accent grabber on hover/drag (handle_resize moved after panel content, takes theme); MenuItem checked flag + MenuBar::set_checked (accent-square indicator); View menu wired — Inspector/Hierarchy/Asset Browser toggle (Scene View/Console disabled), Grid/Colliders checks synced, Reset Layout → EditorContext::reset_layout(); toolbar+play controls follow scene_view_bounds (toolbar_position_for; (220,54) constant gone); EditorPreferences gained PanelPrefs (serde-default for legacy files) + capture/apply_panels; EditorGame loads prefs in init / saves in on_exit (editor_prefs.json); menu Exit → ctx.exit_requested (was process::exit — prefs would've been lost). PHASE B (localization core): engine_core/localization.rs Strings — assets/locales/*.ron LocaleFile v1 tables (corrupt/missing → warn+skip), tr() current→en→key log-once fallback, resolve() for @keys, cycle/available/locale_keys, per-locale font (font_dirty/active_font); GameConfig.locale+locales_dir; ctx.strings on GameContext; GameRunner applies locale fonts after init/update (game/locale_font.rs; base font captured first frame, restored when locale has no font, loaded handles cached); GLYPH CACHES NOW FONT-AWARE — TextDrawData.font_id (sole construction site layout_to_draw_data) + font_id in engine GlyphCacheKey (same-size glyphs from two fonts no longer collide); editor pins its chrome font (re-asserted every frame) and scopes the game view's font to strings.active_font().or(game_base_font) around inner.update; View → "Cycle Game Locale" + status readout; PauseMenuLabels + draw_labeled (Default = English, draw() delegates). PHASE C (data-driven UI): ecs/ui_components.rs — UiAnchor (9-point, ALL/label/index/fraction) + pure resolve_anchored_pos, UiLabel/UiPanel/UiButton (serde defaults everywhere, ComponentMeta, global registry; NO Transform2D — anchor+offset placement); engine_core/ui_element_system.rs draws panels→buttons→labels at the toast slot, @text resolves, presses buffer in GameRunner.pending_ui_events and emit AFTER next frame's flush_events (bus flushes before update — documented 1-frame latency); UiElementsHidden resource suppresses (editor inserts on init/Stop-after-restore, removes on Play); scene schema: 3 ComponentData variants + loader arms (scene_loader_components.rs split) + serializer blocks + 3 round-trip tests; ui crate: keycode_to_char/typed_keys grew A–Z (shift=uppercase)/Space/Shift+Minus='_', NEW text_input widget (float_input's editing model; Some on Enter/Tab/click-away, Escape None) — JUN-T1 CLOSED (→ log_archive; narrowed Low JUN-T1b US-layout-only remains); editor: text_field.rs edit_string + EditableInspector::string_edit (display_string/display_u32 moved), ui_component_editors.rs (anchor via cycle), 3 impl_set_component_command!, ComponentCategory::Ui registry lines, WorldSnapshot capture/restore (play→stop keeps UI components), Entity menu Create UI Label/Panel/Button (entity_ops create_ui_* — Name only). File-size sweeps: menu/{mod,tests}, scene_data_tests.rs + scene_serializer_tests.rs hoisted, game/frame_tail.rs. PHASE D (demos + Pong): examples/assets/locales/{en,pirate}.ron (pirate names BlackSamsGold), hello_world panel fully tr()'d + L-key cycle + 2 scene-defined UiLabels w/ @keys in hello_world.scene.ron (parse covered by existing test), editor_demo with_locales_dir; PONG fully localized — assets/locales/{en,pirate}.ron (title/diff/chaos/HUD/serve/game-over/pause/achievements keys, split around numbers), BlackSamsGold copied to pong assets/fonts, register_all(mgr, strings) w/ ach.<id>.name/.desc keys (re-registration after locale switch refreshes names, id-keyed insert keeps unlocks — tested), title menu gained "Language: {name}" item (5 items), Difficulty::label_key/chaos_label_key, pause via draw_labeled, parity tests (en/pirate key sets equal + every id has name+desc). Workspace: 1237/1237 passed, 0 ignored, clippy --workspace --all-targets clean; pong 11/11 + clippy clean; all touched files ≤600 lines. Docs: AGENTS.md (counts+status), CLAUDE.md for ecs/ui/editor/engine_core/editor_integration + pong, ui TECH_DEBT + root rollup + log_archive. Commits 3b136b3 (A), 878c11f (B), 3b827e4 (C), Phase D + docs in the next commit. Manual GUI verification (collapse/resize feel, locale font swap on screen) left for Jesse — headless coverage is complete.
[2026-07-30] agent-e2 | TASK-E2 | Extracted tile-grid UV math into common::SheetGrid (new sheet_grid.rs, 219 lines, 9 unit tests + 1 doctest); Tilemap::sprite_instances() now delegates via SheetGrid::from_uv_size().uv_rect() — behavior-identical (cell_uv stored verbatim, incl. non-reciprocal 0.3 and degenerate (0,0)->[0,0,0,0] locks; out-of-range passthrough locked per review F5). uv_rect_checked() added for E3/E4. No Tilemap schema/signature change; all tilemap/tilemap_render/frogger locks pass untouched. common/CLAUDE.md file map fixed (math.rs never existed; CameraUniform lives in camera.rs, false renderer-duplication claim dropped). Deviations (approved): no Eq (f32 field, matches Rect), cell_uv private with cols.max(1)/saturating_mul guards. E4 note: Deserialize requires explicit cell_uv; optional-and-derived needs a serde shim, deferred. common 39 tests; workspace 1271/0/0 + clippy clean at time of report (E1/E6 also in tree).
[2026-07-30] agent-e1-e6 | TASK-E6 | Deleted crates/renderer/src/atlas.rs (dead stub, zero consumers) + lib.rs/prelude re-exports + orphaned TextureError::TextureCreationError variant. renderer CLAUDE.md/ANALYSIS.md updated. renderer 75->74 tests (-6 atlas, +E1 additions).
[2026-07-30] agent-e1-e6 | TASK-E1 | TextureFilter knob end-to-end: enum in renderer texture.rs (Linear default, From<TextureFilter> for SamplerConfig sets mag/min/mipmap consistently), re-exported via renderer+engine_core preludes. GameConfig.texture_filter (#[serde(default, with=texture_filter_serde)] bridge — renderer has NO serde dep; Repr mirror avoids remote-derive dead_code) + with_texture_filter(). AssetConfig.default_filter + From<&GameConfig> (headless plumb seam); load_texture delegates to new load_texture_filtered(); create_texture_from_rgba now TextureFilter::Nearest.into() (note: mipmap Linear->Nearest, harmless at mip_level_count 1). game.rs Some/None asset-config branch collapsed, test-locked. engine_core 288->297. Workspace 1271/0/0 + clippy clean, verified independently by coordinator. FLAG: texture.rs at 591/600 lines — extract TextureFilter module before E3/E4.
[2026-07-30] agent-h1 | TASK-H1 | WASM spike VERDICT: YES with caveats — see coordination/H1_SPIKE.md. 14/14 deps compile for wasm32 (incl. rapier2d, fontdue, image exact feature set; wgpu 28 passes even with default features, tuned set = default-features=false + webgpu/wgsl/std). AUDIO DECISION: STAY ON RODIO — OutputStream::try_default() Ok in live browser (cpal built real AudioContext), symphonia decoded WAV on wasm; AudioManager surface mirror compiles clean. Demo verified by screenshot (windowed Firefox, WebGPU, fetched 32x32 texture drawn) — spikes/h1_wasm/, build.sh + port 8777. Roadmap corrections: gilrs needs NO no-op gating (web-sys Gamepad backend exists); wgpu needs no Cargo.toml change. Forced H2 API change: load_sound/play_music must become bytes-primary (std::fs is web-dead). Blockers recorded with file:line for H2-H6 (pollster init, sleep throttle, web-time swap, std::fs sites, canvas-append, 1.83MB baseline -> wasm-opt budget). Autoplay: create OutputStream in gesture handler; try_default() Ok does NOT prove context running. UNVERIFIED BY AGENT: audible sound — Jesse must run the 4-check listen test.
[2026-07-30] jesse | TASK-H1 listen test | PASSED — all 4 audio checks confirmed by ear in Firefox (SFX bus math, seamless loop, live master duck of already-playing music, stop). Audio decision FINAL: stay on rodio. Env note: Firefox dom.webgpu.enabled needs a FULL RESTART to take effect (a new tab is not enough).
[2026-07-30] agent-e3-e4 + coordinator | TASK-E3 + TASK-E4 | SCHEMA FREEZE: named-clip SpriteAnimation + .sheet.ron pipeline, per settled review/plan-v4.md (3 adversarial rounds). E3: `AnimationClip` + reworked `SpriteAnimation { grid: SheetGrid, clips: Vec<(String, AnimationClip)>, sheet, current_clip, playing, current_frame, time_accumulator }` (play/ensure_playing/resume/stop/pause; play() ALWAYS restarts [F4]; arithmetic frame advance — no fps can spin [R2-F1]; fps/empty-clip guards [F1]); SpriteAnimationSystem now writes current_uv() into Sprite.tex_region (two-step borrow) and is DRIVEN from game/frame_tail.rs on delta*time_scale (pause freezes it); render path passes tex_region through ([R3-F3] 4-f32 destructure, default region pixel-identical, test-locked). Scene chain: GridData (cols/rows only) + ONE shared ClipData DTO (scene RON + sidecar [R3-F2], golden wire-format test); autoplay serialized only while playing [R2-F3]; sidecar-as-SSOT on load via TextureResolver::sheet_for/clear_sidecar_cache seam [R3-F4] (headless stubs); autoplay validated vs resolved clips with warning naming clip+sidecar [R3-F9]. Editor: EditorGame freezes engine time outside Play (editor_time_scale, frozen_time_scale hold/restore [R3-F5]) — particles+animations locked still while Editing (time_freeze_tests.rs). E4: sheet_file.rs = .sheet.ron SSOT (SheetFile v1: pixel cell, filter default Nearest [F3], looping default true [R2-F9], fail-loud validation incl. inf/NaN/0/neg fps + empty frames + index-past-grid + zero cell [R3-F7], version check); sidecar_path_for = stem+.sheet.ron (decoy-file test [R2-F2/F8]); texture_filter_serde.rs shared bridge (GameConfig + SheetFile [R3-F1]); assets/sprite_sheet.rs: load_sprite_sheet validates BEFORE loading texture (no handle leak [R2-F10]), SpriteSheet{texture,grid,clips,path}+.animation()/.sprite(); SidecarCache (one read/path/load, warn-and-fall-back on implicit path [F5], cleared at top of SceneLoader::instantiate [R2-F4]); scene texture refs resolve with sidecar filter (Phase G gets Nearest for free). Deviation from plan: SpriteSheet carries `path` (needed to stamp animation.sheet). engine_core +image workspace dep. Docs: training.md Sprite Sheet Pattern, root CLAUDE.md SSOT rows (sheet_file.rs, ClipData), crate CLAUDE.mds, AGENTS.md/roadmap (E3/E4/E9 ☑, freeze REACHED). Tests 1272 -> 1327 (ecs 225, engine_core 334, editor_integration 81), 0 failed/ignored; clippy --workspace --all-targets clean. Verified independently by coordinator.
[2026-07-30] coordinator | E3/E4 code review adjudication | kimi quota-exhausted (403) -> claude fallback reviewer per request-review.sh; review-4.md, verdict Mergeable, 2 Major + 2 Minor. F1 (stopped/static sheet prop reloads showing whole sheet — the E5 tex_region gap made visible) = out of this diff's scope per settled plan; roadmap E5 priority RAISED to before-Phase-F-art. F2 (old-format SpriteAnimation parses silently to inert default) = ACCEPTED: loader now warns on no-sheet-no-clips components naming the entity + test locking the no-error/no-resurrect outcome. F3 (one-frame time-scale slip on toolbar Play/Pause clicks) = accepted as-is, cosmetic (keyboard path unaffected). F4 (new std::fs sites off the H2 ledger) = ACCEPTED: assets/sprite_sheet.rs recorded in H1_SPIKE.md blocker 4. Post-fix gates: 1328/1328, 0 ignored; clippy --all-targets clean.
[2026-07-30] claude | TASK-E5 (round-trip half) | Scene serialization fixes, adversarially reviewed (kimi round 1: F1/F2/F5/F6/F7 rebutted with evidence, F3 already-covered, F4 partial → required test; review/ artifacts). Wire: ComponentData::Sprite gains tex_region (named default (0,0,1,1) — plain serde default would render nothing) + visible (default_true) in scene_data.rs; loader applies instead of hardcoding; serializer writes both. #solid: create_solid_color now records canonical #solid:RRGGBB via new texture_ref::solid_color_path (alpha byte only when translucent; case-safe — parse_hex_color is from_str_radix) so solid-color textures survive save/load (loading #solid: already worked; bare "#solid" from pre-E5 saves still degrades to white+warn). F4 test locks autoplay-overwrites-snapshot on load (clips are the SSOT for animated sprites; tex_region round-trip is for static sheet props). +8 tests (2 parse back-compat/explicit, 2 round-trip incl. F4, 4 solid_color_path). Workspace: 1336/1336 (+8), 0 ignored, clippy --all-targets clean. Docs: roadmap E5 row 🔧 (remaining = F3-gated #rgba save error), capabilities row, TODO.md E5 entry; engine_core CLAUDE.md bullets; Phase I1 website relocated ../games/website/ → ../insiculous_web/ (Jesse's call, sibling folder).
[2026-07-30] claude | TASK-E5 code-review round | kimi code review of the E5 diff (review-2: no regressions; ran tests+clippy itself). F1 ACCEPTED with corrected mechanism: ecs Sprite.tex_region gained #[serde(default)] via named fn (omitted = full texture, matching the scene wire; previously a LOUD missing-field error, not silent-invisible as claimed) + headless partial-JSON test for the future ARCH-006/GPP-06 dynamic-creation path. F2 rebutted (inspector wiring = E8 scope), F3 rebutted (1x1 reload already documented on create_solid_color in-diff). Workspace: 1337/1337 (ecs 226), clippy clean.
[2026-08-19] claude (Fable 5) | TASK-H2+H3+H4+H5(core)+H7(partial)+H9(pong)+I1(staged) | Phase H vertical slice per settled review/plan-v2.md (plan round 1 adjudicated: F1/F5 canonical VFS keys + native test, F2 cfg-split loop, F4/F8/F11/F13-F18 accepted, F3/F6/F7/F10/F12 rebutted w/ evidence). H2: common::clock (std::time native / web_time wasm) swapped in timing, game_loop_manager, lifecycle, achievements. H3 (REVISED per F2): native about_to_wait loop byte-identical; wasm drives frames from RedrawRequested→request_redraw (rAF); shared drive_frame; throttle sleep cfg'd out on wasm; target_fps doc'd native-only; game.rs split → game/app_handler.rs (ApplicationHandler + shutdown, 604→453 lines). H4: RenderManager::init native-only (pollster at outer edge) + shared complete_init; game/web.rs pending_renderer spawn_local + drain + canvas-size push; engine_core wasm deps (wasm-bindgen =0.2.126 etc). H5 core: common::vfs (canonical key = {base}/{entry}, MemFs unit-tested natively incl. locale prefix-scan) consumed by renderer/texture, ui/font, localization (deterministic sorted order), sprite_sheet (bytes dimension probe + exists→read probe), scene_loader; engine_core::web (preload_assets fetch→vfs, init_web_logging, set_boot_status); F13 sweep clean (survivors = persistence/save-side/audio, deliberate). H7 partial: wasm new_or_disabled → always disabled() + rodio wasm-bindgen feature. H9 pong: lib.rs/main.rs/web_entry.rs split (editor stays native-main-only), wasm-release profile, scripts/build_wasm.sh (CLI-version assert w/ remediation, manifest gen, WebGPU-gated test page), 2.5 MiB wasm. DEBUGGING FINDS (roadmap Phase H notes): winit canvas NEVER in DOM → renderer::insert_canvas_into_dom called from WindowManager::create (with_canvas adoption abandoned — detached-canvas black screen, zero errors); no sRGB surface formats on WebGPU → composite shader inv_gamma; 0-size surface clamp + post-adoption resize; wgpu Instance kept alive in Renderer; alpha_mode prefers Opaque. Browser-verified (Playwright full Chromium, headed, real GPU): title menu, mouse click select+confirm, keyboard nav, achievements screen (12 locale entries, 0/12 in-memory), chaos select, gamma-correct brightness. I1 staged in ../insiculous_web: GameEmbed activated (gpu gate BEFORE import, #game-canvas placeholder swap contract, width/height props 800x600), pong.md playable + wasm path, build at public/games/pong/v1/, npm run verify green (incl. a11y 43 pages). PUSH = Jesse's call. Gates: workspace 1268/1268 (+13: common clock/vfs), 0 ignored, clippy --workspace --all-targets clean; pong 11/11 + clippy clean; cargo check -p engine_core --target wasm32 0 warnings (renderer wasm clippy: 3 pre-existing arc_with_non_send_sync, logged in roadmap H8 row). Code-mode adversarial review of the engine diff: IN FLIGHT at log time.
